# minimal-subsequential-transducer
A C++20 implementation of a finite-state pipeline that builds an acyclic subsequential transducer from a finite regular expression over Σ* × Σ* and minimizes it using a worst-case linear-time acyclic automaton minimization algorithm.
